# =============================================================================
# RAG Project Requirements
# =============================================================================
# 安装命令: pip install -r requirements.txt
# 推荐使用清华镜像加速: pip install -r requirements.txt -i https://pypi.tuna.tsinghua.edu.cn/simple
# =============================================================================

# -----------------------------------------------------------------------------
# LangChain 核心
# -----------------------------------------------------------------------------
langchain>=1.0.0
langchain-core>=1.0.0
langchain-openai>=1.0.0
langchain-huggingface>=1.0.0
langchain-community>=0.3.0
langchain-experimental>=0.3.0
langchain-text-splitters>=1.0.0
langchain-tavily>=0.2.0

# -----------------------------------------------------------------------------
# LangGraph 工作流
# -----------------------------------------------------------------------------
langgraph>=1.0.0
langgraph-checkpoint>=4.0.0
langgraph-prebuilt>=1.0.0

# -----------------------------------------------------------------------------
# 向量数据库 - Milvus
# -----------------------------------------------------------------------------
pymilvus>=2.4.0

# -----------------------------------------------------------------------------
# Embeddings 模型
# -----------------------------------------------------------------------------
# 使用本地模型 bge-small-zh-v1.5
sentence-transformers>=3.0.0
transformers>=4.40.0

# PyTorch (CPU版本，体积更小)
# 如果需要GPU支持，移除 --index-url 参数并安装完整版
torch>=2.0.0
# 如需安装CPU专用版本以节省空间，使用:
# pip install torch --index-url https://download.pytorch.org/whl/cpu

# -----------------------------------------------------------------------------
# 文本处理
# -----------------------------------------------------------------------------
tiktoken>=0.5.0
unstructured>=0.15.0
nltk>=3.8.0

# -----------------------------------------------------------------------------
# 工具库
# -----------------------------------------------------------------------------
python-dotenv>=1.0.0
loguru>=0.7.0
pydantic>=2.0.0
pydantic-settings>=2.0.0
requests>=2.31.0
httpx>=0.25.0
tenacity>=8.0.0

# =============================================================================
# 可选依赖 (根据需要取消注释)
# =============================================================================

# -----------------------------------------------------------------------------
# 如果需要 Milvus Lite (轻量级本地向量库)
# -----------------------------------------------------------------------------
# milvus-lite>=2.4.0

# -----------------------------------------------------------------------------
# 如果需要 API 服务
# -----------------------------------------------------------------------------
# fastapi>=0.109.0
# uvicorn>=0.27.0

# -----------------------------------------------------------------------------
# 如果需要 Gradio UI
# -----------------------------------------------------------------------------
# gradio>=4.0.0

# -----------------------------------------------------------------------------
# 如果需要文档解析增强
# -----------------------------------------------------------------------------
# python-docx>=1.0.0      # Word 文档
# python-pptx>=0.6.0      # PPT 文档
# openpyxl>=3.1.0         # Excel 文档
# pypdf>=4.0.0            # PDF 文档
# pdfminer-six>=20231228  # PDF 解析
# beautifulsoup4>=4.12.0  # HTML 解析