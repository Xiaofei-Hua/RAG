# IntentSkill removed: intent classification lives in the chat router
# (api/routers/chat.py via core/intent/classifier.py). This package is retained
# only for its config.yaml/README references; the skill itself was registered
# but never invoked by the graph at runtime.
